{
  "name": "elysium-boost",
  "private": true,
  "scripts": {
    "start": "live-server --port=3000 --host=127.0.0.1 --no-browser",
    "preview": "live-server --port=3000 --host=127.0.0.1 --no-browser"
  },
  "devDependencies": {
    "live-server": "^1.2.2"
  }
}
